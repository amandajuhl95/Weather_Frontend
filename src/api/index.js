import { getCountry, getCity } from "./geography";
import { getCurrentWeather, getSpecificWeather } from "./weather";
import { getCurrentEvent, getSpecificEvent } from "./event";

export default {
  getCountry,
  getCity,
  getCurrentWeather,
  getSpecificWeather,
  getCurrentEvent,
  getSpecificEvent
};
