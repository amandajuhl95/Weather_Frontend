import { getCountry, getCity } from "./geography"
import { getCurrentWeather, getSpecificWeather } from "./weather"

export default {
  getCountry,
  getCity,
  getCurrentWeather,
  getSpecificWeather
}