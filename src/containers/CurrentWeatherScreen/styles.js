import { makeStyles } from "@material-ui/core/styles"

export default makeStyles(theme => ({
  container: {
    position: 'relative',
    flex: 1,
    display: "flex",
    justifyContent: "center",
    alignItems: "center"
  }
}))
